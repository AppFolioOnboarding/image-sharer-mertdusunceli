<p>
  <strong>Image: <%= @image.id %></strong>

  <br>
  <%= image_tag(@image.image_url, :class => "small-picture")  %>
  <br>
  <% if @image.tag_list.any? %>
    <p>Tags</p>
    <% @image.tag_list.each do |tag| %>
      <%= link_to tag, tagged_path(tag: tag) %>
      <br>
    <% end %>
  <% end %>
</p>

<%= link_to 'Back', images_path %>
